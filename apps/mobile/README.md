# Sihati mobile app placeholder

This directory is reserved for the future Flutter mobile application.

Do not add Flutter screens or mobile features until the mobile API contracts and authentication strategy are approved. For the current plan, see:

- `docs/mobile/mobile-roadmap.md`
- `docs/mobile/mobile-architecture.md`

The existing Next.js web app intentionally remains at the repository root for now because moving it to `apps/web` requires a dedicated migration branch with full CI, Docker, Prisma, path-alias, and deployment validation.
